@{
    ViewBag.Title = "Index";
}


<div class="row">
    <div class="col-12">
        <h3>Car List</h3>
        <div class="d-flex justify-content-sm-end mb-1">
            @Html.ActionLink("Create", "Create", "MyCars", new { }, new { @class = "btn btn-primary btn-sm" })
        </div>
        @Html.Action("CarList", "MyCars")
    </div>
</div>
